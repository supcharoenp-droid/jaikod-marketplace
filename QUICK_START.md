# 🚀 JaiKod - Quick Start Guide

## 📂 ไฟล์ที่สร้างให้

### 1. **START_ALL.bat** ⭐ (แนะนำ)
**คลิกเดียว ทำงานทุกอย่าง!**

**ทำอะไร:**
- ✅ เริ่ม Development Server
- ✅ เปิด Browser ทุกหน้าทดสอบ Admin
- ✅ พร้อมใช้งานทันที

**วิธีใช้:**
```
1. Double-click "START_ALL.bat"
2. รอ 10 วินาที
3. Browser จะเปิดอัตโนมัติ
4. เริ่มทดสอบได้เลย!
```

---

### 2. **START_SERVER.bat**
**เริ่มแค่ Server อย่างเดียว**

**ทำอะไร:**
- ✅ เริ่ม Development Server
- ✅ แสดงสถานะใน Command Prompt

**วิธีใช้:**
```
1. Double-click "START_SERVER.bat"
2. รอจนเห็น "Ready in XXXms"
3. เปิด Browser เอง: http://localhost:3000
```

---

### 3. **OPEN_ADMIN_TESTS.bat**
**เปิดหน้าทดสอบ Admin (ต้องเปิด Server ก่อน)**

**ทำอะไร:**
- ✅ เปิด Browser ทุกหน้าทดสอบ Admin

**วิธีใช้:**
```
1. เริ่ม Server ก่อน (START_SERVER.bat)
2. Double-click "OPEN_ADMIN_TESTS.bat"
3. Browser จะเปิด 4 แท็บ
```

---

## 🎯 แนะนำการใช้งาน

### **สำหรับการทำงานปกติ:**
```
👉 ใช้ START_ALL.bat
   - คลิกเดียว ทำงานทุกอย่าง
   - ประหยัดเวลา
```

### **สำหรับการพัฒนา:**
```
👉 ใช้ START_SERVER.bat
   - เปิด Server ทิ้งไว้
   - เปิด Browser เอง
   - ดู Log ได้ชัดเจน
```

---

## 📋 หน้าที่จะเปิดอัตโนมัติ

เมื่อใช้ **START_ALL.bat** หรือ **OPEN_ADMIN_TESTS.bat**:

1. **Landing Page**
   - URL: `http://localhost:3000/test-admin`
   - แสดง: คำแนะนำและลิงก์ทุกหน้า

2. **Dashboard**
   - URL: `http://localhost:3000/test-admin/dashboard`
   - แสดง: สถิติ + Quick Actions

3. **Users Management**
   - URL: `http://localhost:3000/test-admin/users`
   - แสดง: ตาราง Users + Search/Filter

4. **Sellers Management**
   - URL: `http://localhost:3000/test-admin/sellers`
   - แสดง: ตาราง Sellers + KYC Status

---

## ⚙️ การตั้งค่า (ถ้าต้องการ)

### เปลี่ยน Port (ถ้า 3000 ถูกใช้แล้ว)

**แก้ไขไฟล์:** `package.json`
```json
{
  "scripts": {
    "dev": "next dev -p 3001"  // เปลี่ยนเป็น 3001
  }
}
```

**แก้ไขไฟล์ .bat ทั้งหมด:**
```batch
REM เปลี่ยนจาก
start http://localhost:3000/test-admin

REM เป็น
start http://localhost:3001/test-admin
```

---

## 🐛 Troubleshooting

### ❌ "npm: command not found"
**วิธีแก้:**
1. ติดตั้ง Node.js: https://nodejs.org
2. Restart Computer
3. ลองใหม่

### ❌ "Port 3000 already in use"
**วิธีแก้:**
1. ปิดโปรแกรมที่ใช้ Port 3000
2. หรือเปลี่ยน Port (ดูด้านบน)

### ❌ Browser ไม่เปิด
**วิธีแก้:**
1. เปิด Browser เอง
2. ไปที่: `http://localhost:3000/test-admin`

### ❌ "node_modules not found"
**วิธีแก้:**
1. Script จะติดตั้งให้อัตโนมัติ
2. หรือรันเอง: `npm install`

---

## 📝 Tips & Tricks

### 1. สร้าง Desktop Shortcut
```
1. Right-click "START_ALL.bat"
2. Send to → Desktop (create shortcut)
3. คลิกจาก Desktop ได้เลย
```

### 2. Pin to Taskbar
```
1. Right-click "START_ALL.bat"
2. Pin to Taskbar
3. เข้าถึงง่ายขึ้น
```

### 3. Auto-Start on Boot (ถ้าต้องการ)
```
1. Win + R → shell:startup
2. Copy "START_ALL.bat" ไปวาง
3. เปิดเครื่องทีไร Server เริ่มเอง
```

---

## 🎉 สรุป

**เมื่อเปิดเครื่องใหม่:**
```
1. Double-click "START_ALL.bat"
2. รอ 10 วินาที
3. เริ่มทำงานได้เลย!
```

**ง่ายแค่นี้!** 🚀

---

## 📞 ต้องการความช่วยเหลือ?

หากมีปัญหา:
1. เช็ค Error Message ใน Command Prompt
2. Screenshot แล้วแจ้ง
3. ระบุ:
   - Windows Version
   - Node.js Version (`node -v`)
   - Error Message

---

**สร้างเมื่อ:** 7 ธันวาคม 2024
**เวอร์ชัน:** 1.0.0
