# Delete All Products - User Guide

## ⚠️ คำเตือนสำคัญ

การลบข้อมูลสินค้าทั้งหมดเป็นการกระทำที่ **ไม่สามารถย้อนกลับได้**  
กรุณาอ่านคู่มือนี้อย่างละเอียดก่อนดำเนินการ

---

## 🎯 วิธีการลบข้อมูล

### วิธีที่ 1: ผ่าน Admin UI (แนะนำ)

1. เปิดเบราว์เซอร์และไปที่:
   ```
   http://localhost:3000/admin/products/delete-all
   ```

2. อ่านคำเตือนอย่างละเอียด

3. พิมพ์ `DELETE ALL` ในช่องยืนยัน

4. คลิกปุ่ม "🗑️ ลบสินค้าทั้งหมด"

5. ยืนยันอีกครั้งในกล่องข้อความ popup

6. รอจนกว่าระบบจะลบข้อมูลเสร็จสิ้น

7. ระบบจะแสดงผลลัพธ์:
   - ✅ สำเร็จ: แสดงจำนวนสินค้าที่ลบ
   - ❌ ล้มเหลว: แสดงข้อความ error

---

### วิธีที่ 2: ผ่าน API (สำหรับ Developer)

**Using cURL:**
```bash
curl -X DELETE http://localhost:3000/api/admin/products/delete-all
```

**Using Postman:**
- Method: `DELETE`
- URL: `http://localhost:3000/api/admin/products/delete-all`
- Headers: (ถ้ามี authentication)

**Response:**
```json
{
  "success": true,
  "message": "All products have been deleted successfully",
  "deletedCount": 55
}
```

---

### วิธีที่ 3: ผ่าน Script (Advanced)

**ไม่แนะนำ** - ใช้ API route แทน

---

## 📊 ข้อมูลที่จะถูกลบ

การลบข้อมูลจะส่งผลกระทบต่อ:

### ✅ ข้อมูลที่จะถูกลบ
- ✅ สินค้าทั้งหมดใน Firestore collection `products`
- ✅ ข้อมูลสินค้าทุกฟิลด์ (ชื่อ, ราคา, รูปภาพ, ฯลฯ)
- ✅ Metadata ของสินค้า

### ❌ ข้อมูลที่ไม่ถูกลบ
- ❌ ข้อมูลผู้ใช้ (Users)
- ❌ ข้อมูลหมวดหมู่ (Categories)
- ❌ ข้อมูล Orders (ถ้ามี)
- ❌ รูปภาพใน Firebase Storage (ต้องลบแยก)

---

## 🔄 ขั้นตอนหลังการลบ

### 1. ตรวจสอบว่าข้อมูลถูกลบหมดแล้ว

**ผ่าน Firebase Console:**
1. ไปที่ [Firebase Console](https://console.firebase.google.com)
2. เลือก Project ของคุณ
3. ไปที่ Firestore Database
4. ตรวจสอบ collection `products` ควรว่างเปล่า

**ผ่าน Application:**
1. ไปที่หน้าแรก: `http://localhost:3000`
2. ควรไม่มีสินค้าแสดง
3. ไปที่หน้า Admin: `http://localhost:3000/admin/products`
4. ควรแสดง "ไม่มีสินค้า"

---

### 2. เริ่มสร้างสินค้าใหม่

ตอนนี้คุณสามารถสร้างสินค้าใหม่ด้วยระบบหมวดหมู่ใหม่:

**ระบบหมวดหมู่ใหม่:**
- 24 หมวดหมู่หลัก
- 150+ หมวดหมู่ย่อยระดับ 2
- 200+ หมวดหมู่ย่อยระดับ 3
- รวม 370+ หมวดหมู่

**วิธีสร้างสินค้าใหม่:**

1. **ผ่าน Snap & Sell (AI):**
   ```
   http://localhost:3000/sell
   ```
   - ถ่ายรูปสินค้า
   - AI จะเติมข้อมูลอัตโนมัติ
   - ตรวจสอบและแก้ไข
   - เผยแพร่

2. **ผ่าน Manual Form:**
   ```
   http://localhost:3000/sell/manual
   ```
   - เลือกหมวดหมู่
   - กรอกข้อมูลสินค้า
   - อัปโหลดรูปภาพ
   - เผยแพร่

---

## 🛡️ มาตรการป้องกัน

### ก่อนลบข้อมูล

1. **สำรองข้อมูล (Backup)**
   - Export ข้อมูลจาก Firestore
   - บันทึก screenshot
   - Export เป็น JSON/CSV

2. **ตรวจสอบสิทธิ์**
   - ตรวจสอบว่าคุณมีสิทธิ์ Admin
   - ตรวจสอบว่าเป็น environment ที่ถูกต้อง (Dev/Production)

3. **แจ้งทีม**
   - แจ้งทีมงานก่อนลบข้อมูล
   - ตรวจสอบว่าไม่มีใครกำลังใช้งาน

---

## 🔧 Troubleshooting

### ปัญหา: ไม่สามารถลบข้อมูลได้

**สาเหตุที่เป็นไปได้:**

1. **ไม่มีสิทธิ์เข้าถึง Firestore**
   - ตรวจสอบ Firebase credentials
   - ตรวจสอบ Firestore Rules

2. **Network Error**
   - ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
   - ตรวจสอบ Firebase project status

3. **Timeout**
   - ข้อมูลมีจำนวนมากเกินไป
   - ระบบใช้ batch delete (500 รายการ/batch)
   - รอให้ process เสร็จสิ้น

---

### ปัญหา: ลบไปแล้วแต่ยังมีสินค้าเหลืออยู่

**วิธีแก้:**

1. Refresh หน้าเว็บ (Ctrl+F5)
2. Clear browser cache
3. ตรวจสอบ Firestore Console โดยตรง
4. ลองลบอีกครั้ง

---

### ปัญหา: Error "Permission Denied"

**วิธีแก้:**

1. ตรวจสอบ Firestore Security Rules:
   ```javascript
   // ใน Firebase Console > Firestore > Rules
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /products/{productId} {
         allow delete: if request.auth != null && 
                       request.auth.token.admin == true;
       }
     }
   }
   ```

2. ตรวจสอบว่า user ที่ login มีสิทธิ์ admin

---

## 📝 Logs และ Monitoring

### ดู Logs

**ใน Browser Console:**
```
F12 > Console
```

**ใน Terminal (Next.js):**
```
npm run dev
```

**ใน Firebase Console:**
```
Firebase Console > Functions > Logs
```

---

## 🎯 Use Cases

### Use Case 1: Reset Database สำหรับ Development
```
1. ลบข้อมูลเก่า
2. สร้างข้อมูลทดสอบใหม่
3. Test features
```

### Use Case 2: เปลี่ยนโครงสร้างหมวดหมู่
```
1. Export ข้อมูลเก่า
2. ลบข้อมูลทั้งหมด
3. Import ข้อมูลใหม่ด้วยหมวดหมู่ใหม่
```

### Use Case 3: Clean Start สำหรับ Production
```
1. สำรองข้อมูล
2. ลบข้อมูล demo/test
3. เริ่มต้นด้วยข้อมูลจริง
```

---

## ✅ Checklist ก่อนลบข้อมูล

- [ ] อ่านคู่มือนี้ทั้งหมด
- [ ] สำรองข้อมูลแล้ว (ถ้าจำเป็น)
- [ ] ตรวจสอบ environment (Dev/Production)
- [ ] แจ้งทีมงานแล้ว
- [ ] มีแผนสำหรับสร้างข้อมูลใหม่
- [ ] เข้าใจว่าการกระทำนี้ไม่สามารถย้อนกลับได้

---

## 📞 ติดต่อ Support

หากพบปัญหาหรือมีคำถาม:
- ตรวจสอบ documentation ใน `/docs`
- ดู error logs
- ติดต่อทีม Development

---

**Last Updated:** 2025-12-07  
**Version:** 1.0  
**Status:** ✅ Ready to Use
