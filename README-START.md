# 🎯 JaiKod.com - Quick Start Guide

## 📦 ไฟล์สำหรับรันโปรแกรม

โปรเจกต์นี้มีไฟล์ batch scripts สำหรับใช้งานง่าย ๆ บน Windows:

### 🚀 ไฟล์หลัก

| ไฟล์ | คำอธิบาย | เมื่อไหร่ควรใช้ |
|------|----------|-----------------|
| **`start.bat`** | รันโปรแกรมแบบ Development | ใช้เมื่อกำลังพัฒนาโปรแกรม (มี Hot Reload) |
| **`start-production.bat`** | Build และรันแบบ Production | ใช้เมื่อต้องการทดสอบ Production หรือใช้งานจริง |

### 🛠️ ไฟล์เสริม

| ไฟล์ | คำอธิบาย | เมื่อไหร่ควรใช้ |
|------|----------|-----------------|
| **`install.bat`** | ติดตั้ง dependencies เท่านั้น | ใช้เมื่อต้องการติดตั้ง/อัปเดต packages |
| **`clean-rebuild.bat`** | ลบ cache และติดตั้งใหม่ทั้งหมด | ใช้เมื่อพบปัญหาแปลก ๆ หรือต้องการเริ่มต้นใหม่ |
| **`open-browser.bat`** | เปิดเบราว์เซอร์ที่ localhost:3000 | ใช้เมื่อเซิร์ฟเวอร์รันอยู่แล้ว แต่ต้องการเปิดเบราว์เซอร์ใหม่ |

## 🎬 เริ่มต้นใช้งานครั้งแรก

### ขั้นตอนที่ 1: ติดตั้ง Node.js
1. ดาวน์โหลด Node.js จาก https://nodejs.org/ (แนะนำเวอร์ชัน LTS)
2. ติดตั้งตามขั้นตอน
3. เปิด Command Prompt แล้วพิมพ์ `node --version` เพื่อตรวจสอบ

### ขั้นตอนที่ 2: ตั้งค่า Environment Variables
1. แก้ไขไฟล์ `.env.local` (จะถูกสร้างอัตโนมัติเมื่อรัน `start.bat` ครั้งแรก)
2. ใส่ค่า Firebase Configuration ของคุณ

### ขั้นตอนที่ 3: รันโปรแกรม
**ดับเบิลคลิกที่ไฟล์ `start.bat`**

หรือเปิด Command Prompt ที่โฟลเดอร์โปรเจกต์ แล้วพิมพ์:
```bash
start.bat
```

### ขั้นตอนที่ 4: เปิดเบราว์เซอร์
ไปที่ http://localhost:3000

## 💡 การใช้งานทั่วไป

### สำหรับการพัฒนา (Development)
```bash
# วิธีที่ 1: ดับเบิลคลิกไฟล์
start.bat

# วิธีที่ 2: ใช้ Command Line
npm run dev
```

### สำหรับ Production
```bash
# วิธีที่ 1: ดับเบิลคลิกไฟล์
start-production.bat

# วิธีที่ 2: ใช้ Command Line
npm run build
npm run start
```

### เมื่อพบปัญหา
```bash
# ดับเบิลคลิกไฟล์
clean-rebuild.bat

# หรือใช้ Command Line
rmdir /s /q .next
rmdir /s /q node_modules
npm install
```

## 🔧 คำสั่ง npm ที่มีอยู่

```bash
npm run dev          # รันเซิร์ฟเวอร์ Development (พอร์ต 3000)
npm run build        # Build โปรเจกต์สำหรับ Production
npm run start        # รันเซิร์ฟเวอร์ Production
npm run lint         # ตรวจสอบโค้ดด้วย ESLint
```

## 📁 โครงสร้างโปรเจกต์

```
jaikod/
├── src/                    # โค้ดหลักของแอพพลิเคชัน
│   ├── app/               # Next.js App Router
│   ├── components/        # React Components
│   ├── lib/               # Utility functions และ Firebase config
│   └── constants/         # ค่าคงที่ต่าง ๆ
├── public/                # Static files
├── .env.local            # Environment variables (ไม่ commit)
├── .env.example          # ตัวอย่าง environment variables
├── start.bat             # รันโปรแกรม Development
├── start-production.bat  # รันโปรแกรม Production
├── install.bat           # ติดตั้ง dependencies
├── clean-rebuild.bat     # ล้าง cache และติดตั้งใหม่
└── open-browser.bat      # เปิดเบราว์เซอร์
```

## 🆘 แก้ไขปัญหาที่พบบ่อย

### ❌ ปัญหา: "ไม่พบ Node.js"
**วิธีแก้:**
1. ติดตั้ง Node.js จาก https://nodejs.org/
2. รีสตาร์ท Command Prompt
3. ลองรันใหม่

### ❌ ปัญหา: "Port 3000 is already in use"
**วิธีแก้:**
1. ปิดโปรแกรมที่ใช้พอร์ต 3000 อยู่
2. หรือเปลี่ยนพอร์ตโดยรัน: `set PORT=3001 && npm run dev`

### ❌ ปัญหา: "Module not found" หรือ "Cannot find package"
**วิธีแก้:**
1. รัน `clean-rebuild.bat`
2. หรือลบโฟลเดอร์ `node_modules` และรัน `npm install` ใหม่

### ❌ ปัญหา: "Firebase Configuration Error"
**วิธีแก้:**
1. ตรวจสอบไฟล์ `.env.local`
2. ตรวจสอบว่าใส่ค่า Firebase Config ครบถ้วน
3. ตรวจสอบว่าไม่มีช่องว่างหรืออักขระพิเศษในค่า config

### ❌ ปัญหา: หน้าเว็บแสดงผลไม่ถูกต้อง
**วิธีแก้:**
1. ล้าง Browser Cache (Ctrl + Shift + Delete)
2. รัน `clean-rebuild.bat`
3. ลองใช้ Incognito/Private Mode

### ❌ ปัญหา: ไฟล์ .bat รันไม่ได้
**วิธีแก้:**
1. คลิกขวาที่ไฟล์ → Properties
2. ตรวจสอบว่าไม่ถูก Block โดย Windows
3. ถ้ามี "Unblock" ให้คลิก Unblock

## 📚 เอกสารเพิ่มเติม

- [Next.js Documentation](https://nextjs.org/docs)
- [Firebase Documentation](https://firebase.google.com/docs)
- [React Documentation](https://react.dev/)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)

## 🎨 เทคโนโลยีที่ใช้

- **Framework:** Next.js 14 (App Router)
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **Backend:** Firebase (Firestore, Storage, Authentication)
- **State Management:** Zustand
- **Animations:** Framer Motion
- **Icons:** Lucide React

## 📞 ติดต่อ & สนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Console ใน Browser (F12)
2. ตรวจสอบ Terminal ที่รันเซิร์ฟเวอร์
3. อ่านเอกสารใน `วิธีการรัน.md`

---

**JaiKod.com** - AI-Native Hybrid Marketplace 🛍️✨

Made with ❤️ in Thailand 🇹🇭
