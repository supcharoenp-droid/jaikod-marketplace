# 🔧 Error Fix: Webpack Cache Issue

## ❌ ปัญหาที่พบ

**Error:** `Cannot read properties of undefined (reading 'call')`

**สาเหตุ:**
- เพิ่ม components ใหม่ (FeaturedSellerBanner)
- Webpack cache ยังเก็บ bundle เก่าไว้
- Next.js ไม่ rebuild ใหม่

## ✅ การแก้ไข

```bash
# ลบ .next cache
Remove-Item -Path ".next" -Recurse -Force
```

**Status:** ✅ เสร็จแล้ว

---

## 🚀 ขั้นตอนต่อไป

### 1. Restart Dev Server

**Terminal 1 (กด Ctrl+C ถ้ายังรันอยู่):**
```bash
npm run dev
```

**รอจนเห็น:**
```
✓ Ready in 3.5s
○ Local: http://localhost:3000
```

### 2. Refresh Browser

```
1. เปิด browser
2. ไป http://localhost:3000
3. กด Ctrl+Shift+R (Hard refresh)
```

### 3. ตรวจสอบ

**ควรเห็น:**
- ✅ Homepage โหลดสำเร็จ
- ✅ Featured Seller Banner แสดง
- ✅ ไม่มี error ใน console

---

## 📊 Expected Result

```
Homepage Layout:

┌────────────────────────────────┐
│  Header                        │
├────────────────────────────────┤
│  Hero + Search                 │
├────────────────────────────────┤
│  Categories (22 หมวดหมู่)      │
├────────────────────────────────┤
│  🌟 FEATURED SELLER BANNER     │  ← ใหม่!
│  ┌──────────────────────────┐ │
│  │ JaiStar Premium Shop     │ │
│  │ Stats, Badges, CTA       │ │
│  └──────────────────────────┘ │
├────────────────────────────────┤
│  New Arrivals                  │
│  ...                           │
└────────────────────────────────┘
```

---

## 🔍 Troubleshooting

### ถ้ายังมี Error:

**1. ตรวจสอบ Terminal:**
```
- มี compilation error ไหม?
- มี warning สีแดงไหม?
```

**2. เช็ค Console:**
```
F12 → Console tab
- มี error ไหม?
- บรรทัดไหน?
```

**3. ลอง Clear Browser Cache:**
```
Ctrl+Shift+Delete → Clear cache
```

**4. Restart ทั้งหมด:**
```bash
# Kill all Node processes
taskkill /F /IM node.exe

# Start fresh
npm run dev
```

---

## ✅ Summary

**ที่ทำไปแล้ว:**
- ✅ ลบ `.next` cache
- ⏳ รอ restart dev server
- ⏳ รอ test ใหม่

**ที่ต้องทำ:**
1. Restart `npm run dev`
2. Refresh browser
3. ตรวจสอบว่า banner แสดง

---

**พร้อมต่อเมื่อ dev server รันแล้ว!** 🚀
