# 📬 Notifications Page - คู่มือการใช้งาน

## 🎯 URL:
```
http://localhost:3000/notifications
```

---

## ✨ ฟีเจอร์ทั้งหมด:

### ✅ **1. รายการการแจ้งเตือนทั้งหมด**
- แสดงการแจ้งเตือนทั้งหมดของผู้ใช้
- เรียงตามเวลาล่าสุด
- แสดงจำนวนที่ยังไม่ได้อ่าน

### ✅ **2. แยกตาม Type**
- **ทั้งหมด** - แสดงทุกประเภท
- **คำสั่งซื้อ** (Order) - การแจ้งเตือนเกี่ยวกับคำสั่งซื้อ
- **ข้อความ** (Message) - ข้อความจากผู้ซื้อ/ผู้ขาย
- **โปรโมชั่น** (Promotion) - ส่วนลด, โปรโมชั่น
- **ระบบ** (System) - การแจ้งเตือนจากระบบ

### ✅ **3. Mark as Read/Unread**
- ปุ่ม "อ่านทั้งหมด" ที่ header
- ปุ่ม eye icon ที่แต่ละรายการ
- แสดง dot สีม่วงสำหรับรายการที่ยังไม่ได้อ่าน

### ✅ **4. ลบการแจ้งเตือน**
- ปุ่ม trash icon ที่แต่ละรายการ
- แสดง loading ขณะลบ
- Animation เมื่อลบ

### ✅ **5. Filter by Type**
- Tab filter ด้านบน
- แสดงจำนวนแต่ละประเภท
- Highlight tab ที่เลือก

### ✅ **6. Real-time Updates**
- ใช้ `subscribeToNotifications` จาก Firebase
- อัปเดตทันทีเมื่อมีการแจ้งเตือนใหม่
- ไม่ต้อง refresh หน้า

---

## 🎨 UI Components:

### **Header:**
```
┌─────────────────────────────────────────┐
│ 🔔 การแจ้งเตือน          [อ่านทั้งหมด]  │
│ 5 รายการที่ยังไม่ได้อ่าน                │
│                                         │
│ [ทั้งหมด] [คำสั่งซื้อ] [ข้อความ] ...   │
└─────────────────────────────────────────┘
```

### **Notification Card:**
```
┌─────────────────────────────────────────┐
│ [Icon] คำสั่งซื้อใหม่ #12345        [•] │
│        มีคำสั่งซื้อใหม่รอการยืนยัน      │
│        🕐 5 นาทีที่แล้ว [คำสั่งซื้อ]   │
│        ─────────────────────────────    │
│        ดูรายละเอียด →                   │
└─────────────────────────────────────────┘
```

---

## 🎨 Color Coding:

### **Order (คำสั่งซื้อ):**
- Color: Blue
- Icon: Package
- Example: "คำสั่งซื้อใหม่", "สถานะการจัดส่ง"

### **Message (ข้อความ):**
- Color: Green
- Icon: MessageCircle
- Example: "ข้อความใหม่จากผู้ซื้อ"

### **Promotion (โปรโมชั่น):**
- Color: Purple
- Icon: Tag
- Example: "ส่วนลด 50%", "โปรโมชั่นพิเศษ"

### **System (ระบบ):**
- Color: Orange
- Icon: AlertCircle
- Example: "อัปเดตระบบ", "การแจ้งเตือนจากแอดมิน"

---

## 🔧 Functions:

### **1. subscribeToNotifications**
```typescript
useEffect(() => {
    const unsubscribe = subscribeToNotifications(user.uid, (notifs) => {
        setNotifications(notifs)
    })
    return () => unsubscribe()
}, [user])
```

### **2. markAsRead**
```typescript
const handleMarkAsRead = async (notificationId: string) => {
    await markAsRead(notificationId)
}
```

### **3. deleteNotification**
```typescript
const handleDelete = async (notificationId: string) => {
    await deleteNotification(notificationId)
}
```

### **4. Filter**
```typescript
const filteredNotifications = filter === 'all'
    ? notifications
    : notifications.filter(n => n.type === filter)
```

---

## 📱 Responsive Design:

### **Mobile:**
- Tabs scroll horizontally
- Cards stack vertically
- Icons และ text ปรับขนาด

### **Desktop:**
- Tabs แสดงทั้งหมด
- Cards กว้างขึ้น
- Hover effects

---

## 🎭 Animations:

### **Card Entry:**
```typescript
initial={{ opacity: 0, y: 20 }}
animate={{ opacity: 1, y: 0 }}
```

### **Card Exit:**
```typescript
exit={{ opacity: 0, x: -100 }}
```

### **Loading:**
- Spinner animation
- Pulse effect สำหรับ unread badge

---

## 🧪 การทดสอบ:

### **1. เปิดหน้า:**
```
http://localhost:3000/notifications
```

### **2. ทดสอบ Filter:**
- คลิก "ทั้งหมด" → แสดงทุกรายการ
- คลิก "คำสั่งซื้อ" → แสดงเฉพาะ order
- คลิก "ข้อความ" → แสดงเฉพาะ message

### **3. ทดสอบ Mark as Read:**
- คลิก eye icon → รายการเปลี่ยนเป็น read
- คลิก "อ่านทั้งหมด" → ทุกรายการเป็น read

### **4. ทดสอบ Delete:**
- คลิก trash icon → รายการถูกลบ
- แสดง loading animation

### **5. ทดสอบ Real-time:**
- เปิด 2 tabs
- สร้างการแจ้งเตือนใหม่
- ทั้ง 2 tabs อัปเดตทันที

---

## 📊 Data Structure:

### **Notification Interface:**
```typescript
interface Notification {
    id: string
    userId: string
    type: 'order' | 'message' | 'promotion' | 'system'
    title: string
    message: string
    isRead: boolean
    createdAt: Timestamp
    actionUrl?: string
    actionText?: string
}
```

---

## 🔗 Integration:

### **จาก Header:**
```typescript
// Header.tsx
<Link href="/notifications">
    <Bell className="w-5 h-5" />
    {unreadCount > 0 && (
        <span className="badge">{unreadCount}</span>
    )}
</Link>
```

### **จาก Profile:**
```typescript
// Profile Sidebar
<Link href="/notifications">
    🔔 การแจ้งเตือน
</Link>
```

---

## ⚡ Performance:

### **Optimizations:**
- ✅ Real-time subscription (ไม่ต้อง polling)
- ✅ AnimatePresence สำหรับ smooth transitions
- ✅ Lazy loading (ถ้ามีเยอะ)
- ✅ Memoization สำหรับ filter

---

## 🎯 Use Cases:

### **Seller:**
- ได้รับแจ้งเตือนคำสั่งซื้อใหม่
- ข้อความจากผู้ซื้อ
- สถานะการจัดส่ง

### **Buyer:**
- สถานะคำสั่งซื้อ
- ข้อความจากผู้ขาย
- โปรโมชั่นพิเศษ

### **Admin:**
- การแจ้งเตือนระบบ
- รายงานปัญหา

---

## 📝 สรุป:

### **ฟีเจอร์:**
- ✅ รายการการแจ้งเตือนทั้งหมด
- ✅ แยกตาม type (4 ประเภท)
- ✅ Mark as read/unread
- ✅ ลบการแจ้งเตือน
- ✅ Filter by type
- ✅ Real-time updates

### **URL:**
```
http://localhost:3000/notifications
```

### **Dependencies:**
- `@/lib/notifications` - Firestore functions
- `framer-motion` - Animations
- `lucide-react` - Icons

---

**พร้อมใช้งานแล้ว!** 🚀

ไปที่ `http://localhost:3000/notifications` เพื่อดูการแจ้งเตือนทั้งหมด!
