# 🚀 วิธีการรันโปรแกรม JaiKod

## 📋 ข้อกำหนดเบื้องต้น

ก่อนรันโปรแกรม ต้องติดตั้งโปรแกรมเหล่านี้ก่อน:

- **Node.js** (เวอร์ชัน 18 ขึ้นไป) - [ดาวน์โหลด](https://nodejs.org/)
- **npm** (มากับ Node.js อยู่แล้ว)

## 🎯 วิธีการรันโปรแกรม

### วิธีที่ 1: รันแบบ Development (แนะนำสำหรับการพัฒนา)

**ดับเบิลคลิกที่ไฟล์:**
```
start.bat
```

หรือเปิด Command Prompt/PowerShell แล้วพิมพ์:
```bash
start.bat
```

โปรแกรมจะ:
- ✅ ตรวจสอบว่ามี Node.js ติดตั้งหรือไม่
- ✅ ติดตั้ง dependencies อัตโนมัติ (ถ้ายังไม่มี)
- ✅ คัดลอกไฟล์ `.env.local` จาก `.env.example` (ถ้ายังไม่มี)
- ✅ เริ่มต้นเซิร์ฟเวอร์ที่ `http://localhost:3000`

### วิธีที่ 2: รันแบบ Production (สำหรับใช้งานจริง)

**ดับเบิลคลิกที่ไฟล์:**
```
start-production.bat
```

หรือเปิด Command Prompt/PowerShell แล้วพิมพ์:
```bash
start-production.bat
```

โปรแกรมจะ:
- ✅ Build โปรเจกต์สำหรับ Production
- ✅ เริ่มต้นเซิร์ฟเวอร์ Production ที่ `http://localhost:3000`

### วิธีที่ 3: รันด้วย npm (สำหรับผู้ที่คุ้นเคยกับ Command Line)

เปิด Command Prompt/PowerShell ที่โฟลเดอร์โปรเจกต์ แล้วพิมพ์:

**Development Mode:**
```bash
npm install        # ติดตั้ง dependencies (ครั้งแรกเท่านั้น)
npm run dev        # รันเซิร์ฟเวอร์
```

**Production Mode:**
```bash
npm install        # ติดตั้ง dependencies (ครั้งแรกเท่านั้น)
npm run build      # Build โปรเจกต์
npm run start      # รันเซิร์ฟเวอร์ Production
```

## ⚙️ การตั้งค่า Environment Variables

1. แก้ไขไฟล์ `.env.local` (จะถูกสร้างอัตโนมัติจาก `.env.example`)
2. ใส่ค่า Firebase Configuration ของคุณ:

```env
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-auth-domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-storage-bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id
```

## 🌐 เข้าถึงเว็บไซต์

หลังจากรันโปรแกรมแล้ว เปิดเบราว์เซอร์และไปที่:

```
http://localhost:3000
```

## 🛑 หยุดเซิร์ฟเวอร์

กด `Ctrl + C` ใน Command Prompt/PowerShell ที่กำลังรันเซิร์ฟเวอร์อยู่

## 📝 หมายเหตุ

- **Development Mode** จะมีการ Hot Reload (รีโหลดอัตโนมัติเมื่อแก้ไขโค้ด)
- **Production Mode** จะมีประสิทธิภาพสูงกว่า แต่ต้อง Build ใหม่ทุกครั้งที่แก้ไขโค้ด
- ถ้าพบปัญหา ลองลบโฟลเดอร์ `node_modules` และ `.next` แล้วรันใหม่

## 🆘 แก้ไขปัญหา

### ปัญหา: ไม่พบ Node.js
**วิธีแก้:** ติดตั้ง Node.js จาก https://nodejs.org/

### ปัญหา: Port 3000 ถูกใช้งานอยู่
**วิธีแก้:** เปลี่ยน port โดยรันคำสั่ง:
```bash
set PORT=3001 && npm run dev
```

### ปัญหา: Firebase Configuration Error
**วิธีแก้:** ตรวจสอบไฟล์ `.env.local` ว่าใส่ค่าครบถ้วนและถูกต้อง

---

**JaiKod.com** - AI-Native Hybrid Marketplace 🛍️✨
