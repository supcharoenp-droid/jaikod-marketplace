# 🔍 System Health Check - คู่มือการใช้งาน

## 📋 ภาพรวม

หน้า **System Health Check** เป็นเครื่องมือสำหรับ Admin ในการตรวจสอบสถานะและการทำงานของระบบ JaiKod ทั้งหมด เพื่อให้มั่นใจว่าระบบทำงานได้อย่างสมบูรณ์

## 🎯 การเข้าถึง

### วิธีที่ 1: จากหน้า Admin Dashboard
1. เข้าสู่ระบบด้วยบัญชี Admin
2. ไปที่ `/admin`
3. คลิกที่การ์ด **"ตรวจสอบระบบ"** (System Health) สีน้ำเงิน

### วิธีที่ 2: เข้าโดยตรง
เปิดเบราว์เซอร์และไปที่:
```
http://localhost:3000/admin/system-check
```

## 🔍 รายการที่ตรวจสอบ

### 1️⃣ Configuration (การตั้งค่า)

#### Environment Variables
- **ตรวจสอบ:** ไฟล์ `.env.local` มีค่าครบถ้วนหรือไม่
- **รายการที่ตรวจ:**
  - `NEXT_PUBLIC_FIREBASE_API_KEY`
  - `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN`
  - `NEXT_PUBLIC_FIREBASE_PROJECT_ID`
  - `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET`
  - `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID`
  - `NEXT_PUBLIC_FIREBASE_APP_ID`

**สถานะที่เป็นไปได้:**
- ✅ **Success:** ตั้งค่าครบถ้วน
- ⚠️ **Warning:** ตั้งค่าไม่ครบ
- ❌ **Error:** ยังไม่ได้ตั้งค่าเลย

---

### 2️⃣ Firebase (บริการ Firebase)

#### Firebase Configuration
- **ตรวจสอบ:** ค่า Config ของ Firebase ถูกต้องหรือไม่
- **แสดงผล:** ค่า Config แต่ละตัว (แสดงบางส่วน)

#### Firestore Database
- **ตรวจสอบ:** การเชื่อมต่อกับ Firestore
- **ทดสอบ:** อ่านข้อมูลจาก Collection `products`
- **แสดงผล:** 
  - สถานะการเชื่อมต่อ
  - จำนวนเอกสารในคอลเลกชัน

#### Firebase Authentication
- **ตรวจสอบ:** ระบบ Authentication ทำงานหรือไม่
- **แสดงผล:**
  - สถานะการเริ่มต้น Auth
  - ผู้ใช้ที่ล็อกอินอยู่ (ถ้ามี)
  - Auth Domain

#### Firebase Storage
- **ตรวจสอบ:** Firebase Storage พร้อมใช้งานหรือไม่
- **ทดสอบ:** ดูรายการไฟล์ในโฟลเดอร์ `/products`
- **แสดงผล:**
  - Storage Bucket
  - จำนวนไฟล์
  - จำนวนโฟลเดอร์ย่อย

---

### 3️⃣ Dependencies (แพ็กเกจที่จำเป็น)

#### NPM Packages
- **ตรวจสอบ:** แพ็กเกจที่จำเป็นติดตั้งครบหรือไม่
- **รายการที่ตรวจ:**
  - `react`
  - `next`
  - `firebase`
  - `framer-motion`
  - `lucide-react`

**สถานะที่เป็นไปได้:**
- ✅ **Success:** ติดตั้งครบทุกแพ็กเกจ
- ⚠️ **Warning:** ขาดบางแพ็กเกจ

---

## 🎨 การใช้งาน

### การตรวจสอบครั้งแรก
เมื่อเปิดหน้าครั้งแรก ระบบจะ:
1. แสดงสถานะ "กำลังตรวจสอบระบบ..."
2. ตรวจสอบทีละรายการตามลำดับ
3. แสดงผลลัพธ์พร้อมสถานะ

### การตรวจสอบใหม่
คลิกปุ่ม **"ตรวจสอบใหม่"** (มุมขวาบน) เพื่อ:
- รันการตรวจสอบใหม่ทั้งหมด
- อัปเดตสถานะล่าสุด
- ตรวจสอบว่ามีการเปลี่ยนแปลงหรือไม่

### การดูรายละเอียด
- คลิกที่การ์ดแต่ละรายการเพื่อดูรายละเอียดเพิ่มเติม
- รายละเอียดจะแสดงข้อมูลเชิงลึก เช่น:
  - ค่า Environment Variables
  - ข้อความ Error (ถ้ามี)
  - ข้อมูลเพิ่มเติมจากการทดสอบ

---

## 📊 สถานะโดยรวม

### ตัวบ่งชี้สถานะ
- 🟢 **ระบบทำงานปกติทั้งหมด:** ผ่านการตรวจสอบทุกรายการ
- 🟡 **พบปัญหาบางส่วน:** มีรายการที่เป็น Warning
- 🔴 **พบข้อผิดพลาด:** มีรายการที่เป็น Error
- 🔵 **กำลังตรวจสอบระบบ:** กำลังรันการตรวจสอบ

### ตัวเลขสรุป
แสดงจำนวนรายการที่ผ่านการตรวจสอบ เช่น:
```
6/6 ผ่านการตรวจสอบ
```

---

## 🛠️ การแก้ไขปัญหา

### ❌ Environment Variables ไม่ครบ
**วิธีแก้:**
1. เปิดไฟล์ `.env.local` ในโฟลเดอร์โปรเจกต์
2. เพิ่มค่าที่ขาดหายไป
3. ตรวจสอบว่าไม่มีค่าที่เป็น `YOUR_XXX`
4. รีสตาร์ทเซิร์ฟเวอร์ (`Ctrl+C` แล้วรัน `start.bat` ใหม่)

### ❌ Firebase Config ไม่ถูกต้อง
**วิธีแก้:**
1. ไปที่ [Firebase Console](https://console.firebase.google.com/)
2. เลือกโปรเจกต์ของคุณ
3. ไปที่ Project Settings > General
4. คัดลอกค่า Config ใหม่
5. อัปเดตในไฟล์ `.env.local`

### ❌ ไม่สามารถเชื่อมต่อ Firestore
**สาเหตุที่เป็นไปได้:**
1. **Firestore Rules ไม่ถูกต้อง**
   - ตรวจสอบไฟล์ `firestore.rules`
   - Deploy rules ใหม่: `firebase deploy --only firestore:rules`

2. **Firebase Config ผิด**
   - ตรวจสอบ Project ID
   - ตรวจสอบ API Key

3. **ยังไม่ได้สร้าง Firestore Database**
   - ไปที่ Firebase Console
   - สร้าง Firestore Database

### ❌ Authentication ไม่ทำงาน
**วิธีแก้:**
1. ตรวจสอบว่าเปิดใช้งาน Authentication ใน Firebase Console
2. ตรวจสอบ Auth Domain ใน `.env.local`
3. ตรวจสอบว่าเปิดใช้งาน Email/Password Provider

### ❌ Storage ไม่ทำงาน
**วิธีแก้:**
1. ตรวจสอบว่าเปิดใช้งาน Storage ใน Firebase Console
2. ตรวจสอบ Storage Rules ในไฟล์ `storage.rules`
3. Deploy rules: `firebase deploy --only storage`

### ⚠️ Packages ไม่ครบ
**วิธีแก้:**
1. รัน `install.bat` หรือ `npm install`
2. ถ้ายังไม่ได้ ลองรัน `clean-rebuild.bat`
3. ตรวจสอบ `package.json` ว่ามี dependencies ครบ

---

## 💡 เคล็ดลับ

### การตรวจสอบเป็นประจำ
- ✅ ตรวจสอบทุกครั้งหลังอัปเดตโค้ด
- ✅ ตรวจสอบก่อน Deploy Production
- ✅ ตรวจสอบเมื่อพบปัญหาแปลก ๆ

### การบันทึกผลการตรวจสอบ
- เวลาตรวจสอบล่าสุดจะแสดงที่ด้านบน
- สามารถ Screenshot ผลการตรวจสอบเพื่อเก็บไว้เป็นหลักฐาน

### การใช้ร่วมกับเครื่องมืออื่น
- ใช้ร่วมกับ Browser DevTools (F12) เพื่อดู Console Errors
- ใช้ร่วมกับ Firebase Console เพื่อตรวจสอบข้อมูล
- ใช้ร่วมกับ Network Tab เพื่อดู API Calls

---

## 🔐 ความปลอดภัย

### ข้อมูลที่แสดง
- ✅ แสดงเฉพาะบางส่วนของ API Keys (20 ตัวอักษรแรก)
- ✅ ไม่แสดงค่าที่เป็นความลับเต็มรูปแบบ
- ✅ เข้าถึงได้เฉพาะ Admin เท่านั้น

### คำแนะนำ
- ⚠️ อย่า Screenshot หน้านี้แล้วแชร์สาธารณะ
- ⚠️ ตรวจสอบว่าไม่มีคนอื่นมองเห็นหน้าจอ
- ⚠️ ออกจากระบบเมื่อใช้งานเสร็จ

---

## 📞 ต้องการความช่วยเหลือ?

### เอกสารที่เกี่ยวข้อง
- 📄 `README-START.md` - คู่มือการเริ่มต้นใช้งาน
- 📄 `วิธีการรัน.md` - วิธีการรันโปรแกรม
- 📄 `PRODUCT_SYSTEM.md` - ระบบสินค้า

### ลิงก์ที่เป็นประโยชน์
- [Firebase Documentation](https://firebase.google.com/docs)
- [Next.js Documentation](https://nextjs.org/docs)
- [Firestore Rules Reference](https://firebase.google.com/docs/firestore/security/get-started)

---

**JaiKod.com** - AI-Native Hybrid Marketplace 🛍️✨

Made with ❤️ in Thailand 🇹🇭
